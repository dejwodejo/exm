[project]
name = "egzamin-notes"
version = "0.1.0"
description = "Dev tooling for the egzamin notes static site"
requires-python = ">=3.11"
dependencies = [
    "livereload>=2.7.1",
]

[dependency-groups]
dev = []
